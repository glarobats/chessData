import org.apache.spark.{SparkConf, SparkContext}

object chessData{
  def main(args: Array[String]): Unit = {
    // δημιουργία RDD από το αρχείο εισόδου
    val inputFile = "input/games.csv"


  }
}